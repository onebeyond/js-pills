import React from 'react';

import Layout from '../../layouts/mainLayout';
import SEO from '../../components/seo';

const NotFoundPage = () => (
  <Layout>
    <SEO title='Pill not found' />
    <div>Pill not found</div>
  </Layout>
);

export default NotFoundPage;
